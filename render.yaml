services:
  - type: web
    name: fintask
    runtime: docker
    dockerfilePath: Dockerfile
    buildCommand: ""
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: LOG_CHANNEL
        value: stderr
